#include <iostream>
#include <string>
using namespace std;

int main() {
	int N;
	string str;
	cin >> N;
	getchar(); //处理掉读取N后的‘\n’字符
	while (N-- && getline(cin, str)) {
		cout << str << endl << endl; // 输出带空格的
	}
	while (cin >> str) {
		cout << str << endl << endl; // 输出不带空格的
	}
	return 0;
}
